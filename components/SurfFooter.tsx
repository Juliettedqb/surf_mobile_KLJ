import { View, StyleSheet, Text } from "react-native";

const SurfFooter = () => {
  return <Text style={styles.container}>SurfFooter</Text>;
};

const styles = StyleSheet.create({
  container: {
    flex: 0,
    backgroundColor: "#1C2942",
    color: "#fff",
  },
});
export default SurfFooter;
